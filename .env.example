# 服务端配置
PORT=3000
# JWT 密钥 - 请修改为随机字符串
JWT_SECRET=change-me-to-a-random-string-2024
# 设备绑定码 - 家长在 App 中输入此码绑定学习机
BIND_CODE=918918
# SQLite 数据库文件路径；部署到 Render 时应放在持久化磁盘目录，例如 /var/data/data.db
DB_PATH=./data.db
